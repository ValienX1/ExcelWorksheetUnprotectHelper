namespace ExcelHelp.core.Models
{
    /// <summary>
    /// Enum to not use strings
    /// </summary>
    public enum AdditionalInfo
    {
        NA = 0,
        NotProtected = 1,
        Protected = 2,
        PasswordProtected = 3,
    }
}
